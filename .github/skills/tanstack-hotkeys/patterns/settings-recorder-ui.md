# Custom Shortcut Settings UI Pattern

---

1. Record shortcut
2. Store in user config
3. Register dynamically

```tsx
useHotkey(userSettings.saveShortcut, saveDoc)
```

Useful for:

* IDE-like apps
* dashboards
* productivity tools
